/** @type {import('next').NextConfig} */
const nextConfig = {
  // 기존 구성에 옵션을 추가합니다.
  // output: "export" 또는 "standalone"으로 설정해야 함
  output: {
    // output 옵션 설정
    export: true
  }
}

module.exports = nextConfig
